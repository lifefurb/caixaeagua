using UnityEngine;

public class ScoreManager: MonoBehaviour{
    /*
    //public QuestionScreenBehavior mQuestionScreenBehavior;
    public int m_Score;

    public void IncrementScore() {
        int score = 0;
        switch (QuestionSingleTon.Instance.m_Difficulty) {
            case Difficulty.EASY:
                score = 10;
                break;
            case Difficulty.NORMAL:
                score = 30;
                break;
            case Difficulty.HARD:
                score = 50;
                break;
        }
        m_Score += score;
        //mQuestionScreenBehavior.ShowAddScoreAnimation(score);
    }

    public void DecrementScore() {
        int score = 0;
        switch (QuestionSingleTon.Instance.m_Difficulty) {
            case Difficulty.EASY:
                score = 2;
                break;
            case Difficulty.NORMAL:
                score = 5;
                break;
            case Difficulty.HARD:
                score = 10;
                break;
        }
        m_Score -= score;
        //mQuestionScreenBehavior.ShowSubScoreAnimation(score);
    }*/
}
